import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.futdata.app',
  appName: 'Fut-Data',
  webDir: 'dist/football2',
  bundledWebRuntime: false
};

export default config;
